# Frontend configuration (copy to .env and fill values in your environment)
# Base URL of the backend API (FastAPI). Example: http://localhost:3001
REACT_APP_API_BASE_URL=

# Optional: site URL (used for auth redirect flows if/when needed)
REACT_APP_SITE_URL=
